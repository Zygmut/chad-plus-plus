package utils;

// Constantes para deteminar el que fase del programa se ha producido el error o
public enum Phase {
    PRE_COMPILER_PHASE,
    LEXICAL_PHASE,
    SYNTACTIC_PHASE,
    SEMANTIC_PHASE,
    CODE_GENERATION_PHASE
}
